<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animalrecordkeeper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ManageGroupsActivity"
            android:label="Manage Groups"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GroupDetail"
            android:label="Group Detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ManageAnimalsActivity"
            android:label="Manage Animals"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AnimalDetail"
            android:label="Animal Record"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FeedingGroups"
            android:label="Feeding Groups"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
        android:name=".FeedingAnimals"
        android:label="Feeding Animals"
        android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FeedingActivity"
            android:label="New Feeding Record"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity" android:label="@string/app_name" android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>